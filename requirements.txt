# ============================================================
# Fara Tutorial Guide RAG — 完整依赖（应用层 + 模型服务端）
# 生成日期: 2026-08-11
#
# 安装方式:
#   pip:    pip install -r requirements.txt
#   conda:  conda env create -f environment.yml
#
#   版本敏感（勿随意升级）:
#   Fara1.5-4B 官方要求: torch>=2.11 / transformers>=5.2 / vllm>=0.19.1
#   vllm 0.20+ / torch 2.11+ 默认 CUDA 13 (cu13) 构建, 需 NVIDIA 驱动 >= 580
#   驱动仅支持 CUDA 12.x 时使用下方 cu12 锁定组合, 否则 vLLM 启动报
#   RuntimeError: The NVIDIA driver on your system is too old (found version 12080)
# ============================================================

# ---- HTTP / Web ----
requests>=2.31
flask>=3.0

# ---- RAG 检索 ----
numpy>=1.26
pymupdf>=1.24            # PDF 解析（代码中 import fitz）
jieba>=0.42              # 中文分词（BM25 关键词切分）
rank-bm25>=0.2.2         # 稀疏检索（BM25）
faiss-cpu>=1.8           # 向量检索（FAISS）
sentence-transformers>=5.0   # BGE-small-zh 嵌入模型
modelscope>=1.18         # 魔搭模型下载（snapshot_download）

# ---- 模型服务端 (vLLM)  ----
vllm==0.19.1
torch==2.10.0
transformers==5.14.1

# ---- 实时网页截图（可选）（demo.py --serve-url / tools/screenshot.py 才需要）----
playwright>=1.40
